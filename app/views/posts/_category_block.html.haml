.category-block
  %ul
    - i = 0
    - @categories.each do |cat|
      - i += 1
      = link_to cat do
        -if cat.posts.last
          %li{style: "background-image: url('#{attachment_url(cat.posts.last, :image, :fill, 100, 100)}'); color: #{cat.tag_color}"}
          = cat.nice_name.upcase
