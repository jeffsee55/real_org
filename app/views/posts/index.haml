.container
  = render "nav"
  = render partial: "shared/page_header", locals: { title: "Posts", q: @q }
  .index
    %ul.index_row
      - @posts.each do |p|
        =render partial: "shared/tile", locals: { p: p, cat: p.categories.first }
= render "footer"
